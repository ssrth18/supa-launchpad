import { Plus, RefreshCw, Search, Settings2, Wifi, WifiOff } from 'lucide-react'
import { Logo } from './Logo'
export function Header({query,setQuery,count,onAdd,onSettings,onRefresh,online}:{query:string;setQuery:(v:string)=>void;count:string;onAdd:()=>void;onSettings:()=>void;onRefresh:()=>void;online:boolean}){return <header className="topbar"><button className="brand" onClick={()=>setQuery('')} aria-label="Go to home"><Logo/><span>Supa</span></button><div className="searchbox"><Search size={17}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search bookmarks..." aria-label="Search bookmarks"/><kbd>⌘ K</kbd><span className="search-count">{count}</span></div><div className="top-actions"><span className={`connection ${online?'online':''}`}>{online?<Wifi size={14}/>:<WifiOff size={14}/>}<span>{online?'Synced':'Offline'}</span></span><button className="icon-button" onClick={onRefresh} title="Refresh"><RefreshCw size={17}/></button><button className="icon-button" onClick={onSettings} title="Settings"><Settings2 size={18}/></button><button className="primary-button compact" onClick={onAdd}><Plus size={17}/><span>Add bookmark</span></button></div></header>}
